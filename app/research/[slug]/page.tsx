import {notFound} from 'next/navigation';import {Header,Footer,CTA} from '../../components';import {researchPosts,site} from '../../data';export function generateStaticParams(){return researchPosts.map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=researchPosts.find(x=>x.slug===slug);return p?{title:`${p.title} | ${site.brand}`,description:p.excerpt,openGraph:{images:[p.thumbnail]}}:{}}export default async function ResearchPost({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=researchPosts.find(x=>x.slug===slug);if(!p)notFound();return <><Header/><main className="section"><article className="container guide-article"><p className="eyebrow">Philippines staffing research · Published {p.published}</p><h1>{p.title}</h1><p className="lead">{p.excerpt}</p><img src={p.thumbnail} alt={`${p.title} research illustration`} width="1200" height="630"/><div className="card">{p.body.map(x=><p key={x}>{x}</p>)}<h2>Sources</h2><ul>{p.citations.map(c=><li key={c.href}><a href={c.href} rel="noreferrer">{c.label}</a></li>)}</ul><h2>Continue the research</h2><p>{p.cta}</p><p>{p.internalLinks.map(l=><a key={l.href} href={l.href} style={{marginRight:16}}>{l.label}</a>)}</p></div></article><CTA/></main><Footer/></>}
